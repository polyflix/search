name: Polyflix Search CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  call-spring-boot-base-workflow:
    uses: polyflix/.github/.github/workflows/spring-ci.yml@main
